function mostraform() {
    const form = document.querySelector(".meu-formulario");
    const mascara = document.querySelector(".mascara-meu-formulario");

    if (form && mascara) {
        form.classList.add("ativo");
        mascara.classList.add("ativo");
    }
}

function fecharForm() {
    const form = document.querySelector(".meu-formulario");
    const mascara = document.querySelector(".mascara-meu-formulario");

    if (form && mascara) {
        form.classList.remove("ativo");
        mascara.classList.remove("ativo");
    }
} function mostraform() {
    const form = document.querySelector(".meu-formulario");
    const mascara = document.querySelector(".mascara-meu-formulario");

    if (form && mascara) {
        form.classList.add("ativo");
        mascara.classList.add("ativo");
    }
}

function fecharForm() {
    const form = document.querySelector(".meu-formulario");
    const mascara = document.querySelector(".mascara-meu-formulario");

    if (form && mascara) {
        form.classList.remove("ativo");
        mascara.classList.remove("ativo");
    }
}